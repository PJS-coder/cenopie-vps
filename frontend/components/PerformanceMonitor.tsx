'use client';

export default function PerformanceMonitor() {
  // Performance monitoring disabled in production for better performance
  // Only useful during development for debugging
  return null;
}